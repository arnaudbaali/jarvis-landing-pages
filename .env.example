# Supabase — for waitlist email storage (free tier)
# Create a "waitlist" table: id (uuid), email (text unique), idea (text), created_at (timestamptz default now())
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
